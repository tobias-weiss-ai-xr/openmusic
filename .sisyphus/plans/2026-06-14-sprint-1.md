# Sprint 1: "Ship the Pipeline"

**Period**: Jun 14 – Jun 27 (2 weeks)
**Target**: First public mix release (Jun 27 — Deep Minimal Dub @125BPM Dm)
**Theme**: Fix what's broken, build what's missing, ship the first mix.

---

## State Assessment

### ✅ What works
- Sound quality overhaul (all 17 items from Tiers 1-3 in `2026-06-13-sound-quality-improvements.md`)
- TypeScript: 175/177 tests passing (17 files)
- Effects chain (Tone.js): all effects wired and tested
- Mix generation: full pipeline AI → effects → FLAC
- YouTube OAuth + upload: working
- `openmusic publish` command exists (generate + render + upload)

### ❌ P0 Blockers
| Issue | Impact |
|-------|--------|
| `openart>=0.1.0` not on PyPI (max 0.0.2 available) | `uv pip install -e ".[dev]"` fails → CI broken → dev onboarding broken |
| `openmusic.video.__init__` eagerly imports `langgraph` | 4 test collection errors → Python test suite cannot run |
| `private/main` ahead of `origin/main` | GitHub CI not running, repo out of sync |

### 🗺️ P1 Gaps
- `openmusic release` command (one-shot generate + render + upload)
- Batch short generation automation (monthly N shorts from mix source)
- First scheduled mix release (Jun 27)

---

## Sprint Backlog

### P0 — Blockers (Week 1 priority)

| # | Task | Effort | Files |
|---|------|--------|-------|
| 1 | **Fix openart dependency** — remove from `[artwork]` group or pin to existing version (0.0.2). Used gracefully via try/except everywhere. | quick | `packages/core/pyproject.toml` |
| 2 | **Fix video module imports** — lazy-load `langgraph` inside functions, not at module level. `openmusic.video` must be importable without langgraph installed. | quick | `video/__init__.py`, `video/graph.py`, `video/state.py`, `video/nodes/*.py` |
| 3 | **Push to origin** — sync `private/main` → `origin/main` | trivial | — |
| 4 | **Green CI** — verify both Python + TypeScript CI pass. Fix any remaining issues uncovered. | medium | `.github/workflows/ci.yml`, test fixes |

### P1 — Content Pipeline (Week 2 priority, before Jun 27)

| # | Task | Effort | Details |
|---|------|--------|---------|
| 5 | **`openmusic release` command** — wraps `generate` → render MP4 → upload YouTube in one shot. Builds on existing `publish` command. | medium | `cli/main.py`, `export/` |
| 6 | **Batch short generation tooling** — generate N shorts at staggered positions from a mix, per category rotation (stoic/devops/ai/graph) | medium | `shorts/` pipeline |
| 7 | **First mix generate + upload** — Jun 27 "Deep Minimal Dub" @125BPM Dm | medium | Production run using CLI |

### P2 — Quality

| # | Task | Effort | Details |
|---|------|--------|---------|
| 8 | **Python test suite clean** — fix 4 video test collection errors, verify all ~529 tests pass | medium | Test configs, fixture setup |
| 9 | **Test coverage for publish/release** — integration tests with mocked subprocess | medium | `tests/test_cli/` |
| 10 | **E2E sound quality verification** — generate a mix, verify all new effects are active (sidechain, M/S, LFO, dual delay, brickwall limiter, dithering) | medium | Integration test |

### P3 — Polish

| # | Task | Effort | Details |
|---|------|--------|---------|
| 11 | **TypeScript build all packages** — currently only `effects` compiled; `patterns` and `cli` not in CI build | quick | tsconfig per package, CI step |
| 12 | **Clean working tree** — `.gitignore` untracked temp/scripts | trivial | `.gitignore` |
| 13 | **README/AGENTS.md updates** — document publish/release commands, new effects | writing | `README.md`, `AGENTS.md` |

---

## Schedule

```
Week 1 (Jun 14-20):
  P0: #1 openart fix → #2 video imports → #3 push → #4 green CI
  P2: #8 test suite clean (can overlap with P0)

Week 2 (Jun 21-27):
  P1: #5 release command → #6 batch shorts → #7 first mix release
  P2: #9-10 test coverage + verification
  P3: #11-13 polish (parallel with P1)
```

**Critical path**: P0 (#1-4) → P1 (#5-7) → Jun 27 release.

---

## Dependencies

| Task | Depends On | Blocks |
|------|-----------|--------|
| 1 (openart) | — | 4 (CI) |
| 2 (video imports) | — | 4 (CI), 8 (tests) |
| 3 (push) | — | 4 (CI) |
| 4 (CI green) | 1, 2, 3 | 7 (release), 8 (tests) |
| 5 (release cmd) | — | 7 (release) |
| 6 (batch shorts) | — | — |
| 7 (first mix) | 4, 5 | — |
| 8 (test suite) | 2 | — |
| 9 (test coverage) | 4, 8 | — |
| 10 (E2E verify) | 4 | — |
| 11 (TS build) | — | — |
| 12 (gitignore) | — | — |
| 13 (docs) | 5, 6 | — |

---

## Success Criteria

- [ ] `uv pip install -e ".[dev]"` succeeds (CI passes)
- [ ] All Python tests pass (0 collection errors, 0 failures)
- [ ] All TypeScript tests pass (175+ passed)
- [ ] `openmusic release --help` shows usage
- [ ] `openmusic release --length 2h --output mix.flac` produces + uploads a mix
- [ ] Batch short generation produces N shorts at staggered positions
- [ ] First mix published to YouTube by Jun 27
