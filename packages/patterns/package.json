{
  "name": "@openmusic/patterns",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "test": "vitest run --config ../../vitest.config.ts",
    "prepublishOnly": "tsc"
  },
  "devDependencies": {
    "vitest": "^4.1.2"
  }
}
